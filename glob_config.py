# —— 硅基流动apikey配置 ——————————————
SILICON_API_KEY = ""

# ── 飞书应用凭证（从飞书开发者后台获取）────────────────
APP_ID = ""
APP_SECRET = ""

# ── 多维表格信息（从表格 URL 中获取）────────────────────
APP_TOKEN = ""
TABLE_ID = ""

# ── 默认值 ─────────────────────────────────────────────
DEFAULT_CATEGORY = "餐饮"            # 当无法智能分类时使用的默认分类
DEFAULT_ACCOUNT = "微信"             # 默认支付账户

# ── AI 模型配置 ─────────────────────────────────────────
MODEL_NAME = "nex-agi/Nex-N2-Pro"
SYSTEM_PROMPT = """
你是一个专业的记账助手，负责从用户输入的文本中提取消费信息。

## 提取规则
1. **金额**：必须是正数（整数或小数），单位默认为元。如果用户消息中包含数字，将其识别为金额。
2. **支付账户**：只能是“微信”、“支付宝”、“现金”三者之一。如果用户未明确提及，默认使用“微信”。
3. **消费分类**：只能是“餐饮”、“交通”、“购物”、“娱乐”、“医疗”、“生活”、“学习”七类之一。根据描述内容判断最合适的分类，无法判断时选择“其他”。
4. **消费描述**：简短的备注，应去除金额部分，保留核心消费内容（如“午餐”、“打车”）。

## 示例
- 用户：“打车花了28” → 金额=28，账户=微信，分类=交通，描述=打车
- 用户：“中午吃肯德基花了35.5，支付宝给的” → 金额=35.5，账户=支付宝，分类=餐饮，描述=午餐吃肯德基
- 用户：“买书” → 金额=缺少？此时应提示无法提取金额，但系统会要求必须输出；模型需识别错误。实际上本任务中用户输入通常包含金额，若没有金额则模型输出应反映缺失，但结构化输出要求必须有 amount，所以此处假设用户总是提供金额。

## 重要约束
- 严格按照 JSON Schema 输出，不要添加额外解释或标记。
- 如果用户消息中的信息不完整（例如缺少金额），请尝试合理推断；若无法推断，金额设为 0。
"""

# ── 重试配置 ─────────────────────────────────────────────
# AI 模型调用
AI_MAX_RETRIES = 2           # AI 解析最多重试次数
AI_BASE_DELAY = 1.0          # 重试基础延迟（秒）
# 飞书 API 调用
API_MAX_RETRIES = 2          # 飞书 API 最多重试次数
API_BASE_DELAY = 0.5         # 重试基础延迟（秒）
# 异步处理
ASYNC_MAX_WORKERS = 5        # 线程池最大并发数

# ── 降级策略 ─────────────────────────────────────────────
# AI 解析失败时是否回退到正则匹配
FALLBACK_TO_REGEX = True